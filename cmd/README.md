# cmd/

Berisi entry point aplikasi. Setiap subfolder adalah satu binary yang bisa di-build.

- `server/` — binary untuk menjalankan HTTP server

Jika nanti ada binary lain (misal CLI tools, worker, migrator), buat subfolder baru di sini.
